export const environment = {
  production: true,
  supabaseUrl: process.env['VITE_SUPABASE_URL'], // Empty for now build-env will place here data
  supabaseKey: process.env['VITE_SUPABASE_KEY'], // Empty for now build-env will place here data
};
